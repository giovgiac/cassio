data
    struct Point {
        dword x;
        dword y;
    };

    struct Point3 {
        dword z;
    };

    Point first;
    Point second;
    qword test;
    qword test2;
    byte test0;
    word test1;

    byte arr (10);
    word vec (15);
code
    function karalho(byte num) <- byte {
        return [num];
    };

    function cassio() {
        [test] <- karalho(30);
        outputi([test]);
    };

