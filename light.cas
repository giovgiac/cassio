data
    struct Point {
        dword x;
        dword y;
    };

    struct Point3 {
        dword z;
    };

    Point first;
    Point second;
    qword test;
    qword test2;
    byte test0;
    word test1;

    byte arr (10);
    word vec (15);
code
    function karalho() {


    };

    function cassio() {
        [test] <- 0;

        while ([test] < 100) {
            [test] <- [test] + 1;
            outputi([test]);
        };

        karalho();
    };

